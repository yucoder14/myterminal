# for now just compile them all at the same time --> should things a object files and link them...
myterminal: frame.h main.h frame.cpp main.cpp terminal.cpp terminal.h grid.cpp grid.h
	c++ main.cpp frame.cpp terminal.cpp ptyData.cpp grid.cpp `wx-config --cxxflags --libs` -std=c++17
